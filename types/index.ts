export type AudioProps = {
  title: string;
  audioUrl: string;
  imageUrl: string;
  author: string;
  podcastId: string;
};

export type NewsArticle = {
  title: string;
  summary: string;
  source: string;
  url: string;
};
